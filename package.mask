##
# Force use of heimdal instead of mit-krb5, to match what samba brings with it internally.
##
app-crypt/mit-krb5

##
# Don't use pre-compiled rust.
##
dev-lang/rust-bin

##
# Don't use pre-compiled dotnet-sdk
##
dev-dotnet/dotnet-sdk-bin
