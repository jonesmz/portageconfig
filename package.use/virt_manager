##
# Needed for virt-manager suff
##
net-dns/dnsmasq script

##
# Just dependencies
##
net-libs/gnutls pkcs11 tools
net-misc/spice-gtk usbredir
dev-libs/libxml2 python

##
# QEMU arm emulation support, and more.
##
app-emulation/qemu static-user numa spice zstd io-uring opengl virgl virtfs bpf
app-emulation/qemu -bzip2 -sdl -alsa -gtk -pulseaudio -curl -snappy -jpeg -png -nls -ncurses -pin-upstream-blobs -slirp -oss -wayland

##
# Needed for cross-compile chroots
##
app-emulation/qemu xattr

##
# Virtual Machine Disk Image Management needs this
##
app-arch/unzip natspec
app-emulation/libguestfs ocaml

